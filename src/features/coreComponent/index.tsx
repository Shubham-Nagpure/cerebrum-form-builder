import { CustomButton as Button } from './Button';
import { CustomForm as Form } from './Form';

/**
 * AllComponents
 *
 * This return all the core component that need to drag
 */
const AllComponents: Record<string, React.FC> = {
  Button,
  Form
};

export default AllComponents;
